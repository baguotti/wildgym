version: '3.8'

services:
  gym-booking:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: office_gym_booking
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - GYM_CAPACITY=3
      - START_HOUR=6
      - END_HOUR=21
      - GYM_NAME=Wild Island Gym
    volumes:
      - gym-data:/app/data

volumes:
  gym-data:
    driver: local
